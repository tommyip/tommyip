<svg fill="none" viewBox="0 0 800 400" width="800" height="400" xmlns="http://www.w3.org/2000/svg">
	<foreignObject width="100%" height="100%">
		<div xmlns="http://www.w3.org/1999/xhtml">
			<style>
        :root {
          font-size: 17px;
        }

        .container {
          border-radius: 8px;
          padding: 2rem;

          font-family: 'Roboto', sans-serif;
          background: linear-gradient(to right, #283048, #859398);
          color: #fff;
          display: grid;
          place-items: center;
        }

        .tagline {
          font-size: 2.5rem;
          font-weight: 300;
          margin-bottom: 1rem;
        }

        .btn {
          font-size: 1.5rem;
          font-weight: 600;
          background-color: #0078D7;
          border: 0;
          border-radius: 5px;
          color: white;
          padding: 1rem 1.5rem;
        }

        .btn:hover {
          transform: translateY(-1px);
        }
			</style>
			<div class="container">
				<div class="tagline">Hi there, I am Thomas 👋</div>
        <a href="https://ipthomas.com" class="btn">Blog</a>
			</div>
		</div>
	</foreignObject>
  <circle cx="250" cy="250" r="50" fill="red" />
  <script type="text/javascript">
  <![CDATA[
    const circle = document.getElementsByTagName("circle")[0];
    const moveSpeed = 5;
    let x = circle.getAttribute('cx')*1;
    let y = circle.getAttribute('cy')*1;
    document.addEventListener('keydown', function(event) {
      switch(event.key) {
      case 'w':
        circle.setAttribute('cy', y-=moveSpeed);
        break;
      case 'a':
        circle.setAttribute('cx', x-=moveSpeed);
        break;
      case 's':
        circle.setAttribute('cy', y+=moveSpeed);
        break;
      case 'd':
        circle.setAttribute('cx', x+=moveSpeed);
        break;
      }
    });
  ]]>
  </script>
</svg>
